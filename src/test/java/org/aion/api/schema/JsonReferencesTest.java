package org.aion.api.schema;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.*;

import org.junit.Test;

public class JsonReferencesTest {
    @Test
    public void testPutGetSizeEmpty() {
//        TypeReferences unit = new TypeReferences();
//        JsonSchemaRef ref = new JsonSchemaRef("ref");
//        assertThat(unit.size(), is(0));
//        assertThat(unit.isEmpty(), is(true));
//        unit.put("type", ref);
//        assertThat(unit.get("type"), is(ref));
//        assertThat(unit.size(), is(1));
//        assertThat(unit.isEmpty(), is(false));
    }

    @Test
    public void testPutDoesNotAllowOverwrite() {
//        TypeReferences unit = new TypeReferences();
//        JsonSchemaRef ref = new JsonSchemaRef("ref");
//
//        unit.put("type", ref);
//        assertThat(unit.get("type"), is(ref));
//
//        unit.put("type", ref);
//        assertThat(unit.get("type"), is(ref));
//
//        unit.put("type", new JsonSchemaRef("differentRef"));
    }
}